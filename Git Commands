git init

git add .
git commit -m "message"

git status
git log
git log --oneline
git diff

git blame

git reset --hard <commit short hash num code>
git revert <commit short hash num code>

git remote add <server name (origin)>
git remote -v
